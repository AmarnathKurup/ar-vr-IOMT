{
  "noise_levels": {
    "low":    { "type": "gaussian", "mean": 0.0, "std_dev": 0.5 },
    "medium": { "type": "gaussian", "mean": 0.0, "std_dev": 2.0 },
    "high":   { "type": "gaussian", "mean": 0.0, "std_dev": 5.0 }
  },
  "sensor_anomalies": {
    "linear_drift": {
      "description": "Sensor output drifts linearly over time",
      "params": { "rate_per_sample": 0.1, "max_drift": 10.0 }
    },
    "spike_artifact": {
      "description": "Random sudden spikes in readings",
      "params": { "probability": 0.05, "magnitude": [5, 15] }
    },
    "baseline_wander": {
      "description": "Slow sinusoidal drift in baseline",
      "params": { "frequency_hz": 0.25, "amplitude": 3.0, "sampling_rate_hz": 1.0 }
    },
    "motion_artifact": {
      "description": "Intermittent drops due to patient movement",
      "params": { "probability": 0.08, "drop_percent": [3, 8] }
    },
    "signal_drop": {
      "description": "Progressive signal degradation",
      "params": { "rate_percent_per_sample": 0.5, "max_drop_percent": 8.0 }
    }
  },
  "attack_scenario": {
    "description": "Placeholder for future attack simulation",
    "enabled": false,
    "types": ["replay", "injection", "spoofing"],
    "note": "No values generated - reserved for future IDS work"
  }
}